var webpack = require('webpack');

module.exports = {
  entry: './js/jquery.beautify.js',

  output: {
    path: './dist/',
    filename: 'jquery.beautify.min.js',
  },

  externals: {
    'jquery': '$',
  },

  plugins: [
    new webpack.optimize.UglifyJsPlugin({
      minimize: true,
      output: {
        ascii_only: true, // This way <meta> with UTF-8 is not mandatory
      },
    }),
  ],
};
